{
  "name": "salvamento",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "docker-compose up -d --force-recreate --build",
    "build:mongo": "docker-compose build mongo",
    "build:mongos": "docker-compose build mongo && docker-compose build mongo2 && docker-compose build mongo3",
    "build:mongo-express": "docker-compose build mongo-express",
    "build:front": "docker-compose build front",
    "build:back": "docker-compose build back",
    "build:rabbit": "docker-compose build rabbitmq",
    "reset": "docker-compose down -v",
    "reset:hard": "docker-compose down --rmi all",
    "restart": "npm stop && npm start",
    "start": "docker-compose up -d --force-recreate",
    "stop": "docker-compose down",
    "init-mongo": "mongo/scripts/init-db-user.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
